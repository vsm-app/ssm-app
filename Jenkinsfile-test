node(){
stage("clone"){
  checkout scm
}
stage("check tag exist"){
 check_tag("1.0.0")
}

}
def check_tag(tagname){
sh """
 if GIT_DIR=.git git tag --list | egrep -q "^\$tagname\$"
then
    echo "Found tag"
else
    echo "Tag not found"
fi
"""
}
